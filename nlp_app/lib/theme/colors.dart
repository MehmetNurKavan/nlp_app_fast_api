import 'dart:ui';

const Color backgroundColor = Color(0xFF302931);    // Koyu zemin rengi
const Color primaryAccent = Color(0xFF824F6F);      // Ana vurgu (mor-bordo)
const Color secondaryAccent = Color(0xFF089FFF);    // Yardımcı vurgu (mavi)
const Color textPrimary = Color(0xFFE2EBF6);         // Ana metin rengi (açık gri/mavi)
const Color textHighlight = Color(0xFFFFFBF4);       // Açık alanlar / vurgulu metin
